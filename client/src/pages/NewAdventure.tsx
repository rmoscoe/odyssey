import React from 'react';

interface NewAdventureProps {
    currentPage: string;
    handlePageChange: (page: string) => void;
}

export default function NewAdventure({ currentPage, handlePageChange }: NewAdventureProps) {
    return (
        <></>
    );
}